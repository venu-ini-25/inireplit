# Required — set in Vercel dashboard
VITE_CLERK_PUBLISHABLE_KEY=pk_live_...

# Dev-only overrides — set these in .env.local (never commit .env.local)
# VITE_SCREENSHOT_MODE=true   # bypasses Clerk auth + DB access check
# VITE_USE_MOCK=true          # uses mock data instead of live API
